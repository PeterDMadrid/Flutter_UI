import 'package:flutter/material.dart';

class SubtractionScreen extends StatelessWidget {
  const SubtractionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("subtraction"));
  }
}
